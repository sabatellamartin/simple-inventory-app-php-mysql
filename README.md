# Simple inventory app written in PHP 

This app using MySql database to store data

## Requeriments
Docker and Docker Compose

## Setup

docker compose build
docker compose up -d

Then, in your browser go to
http://localhost/inventario/index.php